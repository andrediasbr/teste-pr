name: validation

on: 
  pull_request:
    types:
      - opened
      - synchronize
    branches: 
      - develop
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    - name: Run Validation
      run: 
        cat readme.md | grep erro
